{
  "name": "Combined Configuration",
  "description": "Configuration that loads both SampleAgentA for DNS lookups and SampleAgentB for website checking",
  "agents": [
    {
      "name": "SampleAgentA",
      "config_file": "config_SampleAgentA.json"
    },
    {
      "name": "SampleAgentB",
      "config_file": "config_SampleAgentB.json"
    }
  ],
  "additional_prompt_info": {
    "domain_context": "Comprehensive Network and Website Diagnostics",
    "instructions": [
      "When checking a base URL (e.g., https://example.com), you're verifying the server is running",
      "When checking a specific URI (e.g., https://example.com/path/document), you're verifying both the server is running AND the specific document exists",
      "A 404 error means the specific document/URI doesn't exist, but the server might be running",
      "A DNS error means the domain itself doesn't exist",
      "A timeout or connection error means the server is unreachable",
      "When performing DNS lookups, you're verifying that a domain name resolves to IP addresses",
      "An NXDOMAIN error means the domain doesn't exist in DNS",
      "A timeout error means the DNS server didn't respond in time",
      "Multiple IP addresses may be returned for load balancing or redundancy"
    ],
    "error_handling": "If any tool calls resulted in errors, especially DNS errors or website access errors, this means the services are NOT accessible"
  }
}